import { Injectable } from '@nestjs/common';
// import { VenomBotService } from '../venom-bot/venom_bot.service';

@Injectable()
export class MessagesService {
  // constructor(private readonly venomService: VenomBotService) {}
  // async sendMessage(to: string, message: string) {
  //   const response = await this.venomService.sendMessage(to, message);
  //   console.log('response send message message', response);
  //   return response;
  // }
}
